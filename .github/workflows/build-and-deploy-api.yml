name: Build and Deploy
on: 
  pull_request:
    branches:
    - master
  push:
    branches:
    - master

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master

    - name: Build the Docker container image
#      run: docker build -t docker.pkg.github.com/chrispat/actions-container-service/app-services:latest .
      run: docker build -t docker.pkg.github.com/marley-nodejs/MERN-Stack-Front-To-Back-v2.0/api.anketa.info:latest -f Dockerfile ./MERN-Stack-Front-To-Back-v2.0/api

#    - name: Push the image to GPR
#      run: |
#        docker login docker.pkg.github.com -u publisher -p "${GITHUB_PACKAGE_REGISTRY_TOKEN}"
#        docker push docker.pkg.github.com/chrispat/actions-container-service/app-services:latest
#      env:
#        GITHUB_PACKAGE_REGISTRY_TOKEN: ${{ secrets.GITHUB_PACKAGE_REGISTRY_TOKEN }}

